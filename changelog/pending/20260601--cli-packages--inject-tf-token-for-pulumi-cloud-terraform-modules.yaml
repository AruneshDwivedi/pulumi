changes:
- type: feat
  scope: cli/packages
  description: Inject TF_TOKEN_<host> when running `pulumi package add terraform-module` against a Pulumi Cloud address so the embedded `tofu init` authenticates without a separate `terraform login` step
